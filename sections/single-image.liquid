{%- render 'section-spacing-collapsing' -%}
{% if section.settings.enable_img_radius == false %}
  {% capture radius %}
    no-radius
  {% endcapture %}
{% else %}
  {% capture radius %}
    is-radius
  {% endcapture %}
{% endif %}
<style>
  .single-image-content img {
    max-width: 100%;
    height: auto;
  }
  .{{ section.id }}.single-image-content.is-radius img {
    border-radius: 8px;
  }
  .{{ section.id }}.single-image-content.no-radius img {
    border-radius: initial;
  }
  #shopify-section-{{ section.id }} .section.no-padding-bottom{
    padding-block-end: 0;
  }
</style>
<div {% render 'section-properties' %}>
  <div class="section-stack">
    <div class="single-image-content {{ section.id }} {{ radius }}">
      <picture>
        <source media="(max-width: 767px)" srcset="{{ section.settings.single_img_mb | image_url: width: master }}">
        <source media="(min-width: 768px)" srcset="{{ section.settings.single_img_pc | image_url: width: master }}">
        <img src="{{ section.settings.single_img_pc | image_url: width: master }}">
      </picture>
    </div>
  </div>
</div>
{% schema %}
{
  "name": "Single Image",
  "class": "shopify-section--single-image",
  "tag": "section",
  "settings": [
    {
      "type": "image_picker",
      "id": "single_img_pc",
      "label": "PC端图片"
    },
    {
      "type": "image_picker",
      "id": "single_img_mb",
      "label": "移动端图片"
    },
    {
      "type": "checkbox",
      "id": "enable_padding_bottom",
      "label": "图片下边间距",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_img_radius",
      "label": "图片圆角",
      "default": true
    },
    {
      "type": "header",
      "content": "以下为模板基础配置",
      "info": "以下配置为模板自带的snippets文件需要的配置项，为了保持模板风格的统一"
    },
    {
      "type": "checkbox",
      "id": "full_width",
      "label": "Full width",
      "default": true
    },
    {
      "type": "color",
      "id": "background",
      "label": "Background"
    },
    {
      "type": "color_background",
      "id": "background_gradient",
      "label": "Background gradient"
    }
  ],
  "presets": [
    {
      "name": "Single Image",
      "category": "Media"
    }
  ]
}
{% endschema %}
